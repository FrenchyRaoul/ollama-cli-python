# Ollama Configuration
ollama:
  host: "192.168.0.146"
  port: 11434  # Default Ollama port
  model: "qwen2.5-coder:7b"  # Change to your preferred model
  # odel: "phi3:mini"  # much faster, but less accurate
  embedding_model: "nomic-embed-text"  # Model for generating embeddings
  
# Qdrant Configuration
qdrant:
  host: "192.168.0.146"
  port: 6333  # Default Qdrant port
  enabled: true  # Set to false to disable vector search
  
# Request settings
request:
  timeout: 120  # seconds
  stream: false

# Made with Bob
